<p style="color: green"><%= notice %></p>

  <section class='recipe-show' >

  <p class='recipe-name'> <%= @recipe.name %> </p>

  <div>
    <p>
      <strong>Preparation time:</strong>
      <%= @recipe.preparation_time %>
    </p>

    <%=form_with url: recipe_path(@recipe), method: "patch" do %>
            <label for="public-toggle" class="switch">
              <%= check_box_tag 'public' , @recipe.public, @recipe.public, id: "public-toggle" , onchange: "this.form.submit()" %>
              <span class="slider round"></span>
            </label>
    <% end %>
  
  </div>
 
  <p>
    <strong>Cooking time:</strong>
    <%= @recipe.cooking_time %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= @recipe.description %>
  </p>


   <table>
    <thead>
      <tr>
        <th>Food</th>
        <th>Quantity</th>
        <th>Value</th>
        <th>Actions</th>
        </tr>
    </thead>
    <tbody>
      <% @recipe.recipe_foods.each do |food_recipe| %>
      <tr>
        <td><%= food_recipe.food.name %></td>
        <td> <%= food_recipe.food.measurement_unit %> </td>
        <td> <%= food_recipe.food.unit_price*food_recipe.quantity %></td>
        <td> 
        <div>
          <%= link_to "Modify", edit_recipe_food_recipe_path(@recipe.id, food_recipe.id) %> 
          <%= button_to "Remove", recipe_food_recipe_path(@recipe.id, food_recipe.id), :method => :delete %>
        </div>       
        </td>
      </tr>
    <% end %>
   </tbody>
  </table>


  
</section>


























  <%= link_to "Back to recipes", recipes_path %>
